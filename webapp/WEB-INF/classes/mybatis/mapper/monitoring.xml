<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="monitoring">

	<select id="getMonitoringList" parameterType="monitoring"
		resultType="monitoring">
		SELECT *
		FROM v_monitoring
	</select>


	<select id="gettrend" parameterType="monitoring" resultType="monitoring">
    SELECT
        tdatetime,
        bcf1_cf_pv,
        bcf1_oil_pv,
        (bcf1_cp_pv * 0.001) AS bcf1_cp_pv,
        bcf2_cf_pv,
        bcf2_oil_pv,
        (bcf2_cp_pv * 0.001) AS bcf2_cp_pv,
        bcf3_cf_pv,
        bcf3_oil_pv,
        (bcf3_cp_pv * 0.001) AS bcf3_cp_pv,
        bcf4_cf_pv,
        bcf4_oil_pv,
        (bcf4_cp_pv * 0.001) AS bcf4_cp_pv,
        bcf5_cf_pv,
        bcf5_oil_pv,
        bcf5_cp_pv,
        tf1_zone1,
        tf1_zone2,
        tf1_zone3
    FROM tb_temp
    WHERE CONVERT(DATETIME, tdatetime, 120) 
          BETWEEN CONVERT(DATETIME, #{startDate}, 120)
              AND CONVERT(DATETIME, #{endDate}, 120)
    ORDER BY tdatetime
</select>

<!-- 2025-08-18 추가 -->
	<select id="getMonitoringDupChk" parameterType="workjisi" resultType="workjisi">
		SELECT *
		FROM tb_monitoring_real
		WHERE hogi = #{hogi}
		AND lot_qr = #{lot_qr}		
	</select>

	<select id="getMonitoringData" parameterType="workjisi" resultType="workjisi">
		SELECT jisi_lot, jisi_lot_view, prod_name, corp_name,
			CONCAT(now_temp,';',pre) AS pre,
			CONCAT(now_temp,';',chim) AS chim,
			CONCAT(now_temp,';',diff) AS diff,
			CONCAT(now_temp,';',gang) AS gang,
			CONCAT(now_temp,';',cold) AS cold
		FROM (
			SELECT jisi_lot, jisi_lot_view, prod_name, corp_name,
				pre, chim, diff, gang, cold,
				CASE WHEN SUBSTRING(jisi_lot,7,1) = 1 THEN CONCAT('로내온도:',bcf1_cf_pv,';유조온도:',bcf1_oil_pv,';CP:',bcf1_cp_pv)
					 WHEN SUBSTRING(jisi_lot,7,1) = 2 THEN CONCAT('로내온도:',bcf2_cf_pv,';유조온도:',bcf2_oil_pv,';CP:',bcf2_cp_pv)
					 WHEN SUBSTRING(jisi_lot,7,1) = 3 THEN CONCAT('로내온도:',bcf3_cf_pv,';유조온도:',bcf3_oil_pv,';CP:',bcf3_cp_pv)
					 WHEN SUBSTRING(jisi_lot,7,1) = 4 THEN CONCAT('로내온도:',bcf4_cf_pv,';유조온도:',bcf4_oil_pv,';CP:',bcf4_cp_pv)
					 WHEN SUBSTRING(jisi_lot,7,1) = 5 THEN CONCAT('로내온도:',bcf5_cf_pv,';유조온도:',bcf5_oil_pv,';CP:',bcf5_cp_pv)
				END AS now_temp
			FROM (
				SELECT A.jisi_lot, A.jisi_lot_view, C.prod_name, D.corp_name, 
				CONCAT('설정온도:',A.jisi_h_pre_temp,';','설정시간:',A.jisi_h_pre_time,';', CASE WHEN A.jisi_h_pre_cp = NULL THEN '' ELSE CONCAT('설정CP:',A.jisi_h_pre_cp) END) AS pre, 
				CONCAT('설정온도:',A.jisi_h_chim_temp,';','설정시간:',A.jisi_h_chim_time,';',CASE WHEN A.jisi_h_chim_cp = NULL THEN '' ELSE CONCAT('설정CP:',A.jisi_h_chim_cp) END) AS chim,
				CONCAT('설정온도:',A.jisi_h_diff_temp,';','설정시간:',A.jisi_h_diff_time,';',CASE WHEN A.jisi_h_diff_cp = NULL THEN '' ELSE CONCAT('설정CP:',A.jisi_h_diff_cp) END) AS diff,
				CONCAT('설정온도:',A.jisi_h_gang_temp,';','설정시간:',A.jisi_h_gang_time,';',CASE WHEN A.jisi_h_gang_cp = NULL THEN '' ELSE CONCAT('설정CP:',A.jisi_h_gang_cp) END) AS gang,
				CONCAT('설정온도:',A.jisi_h_cold_temp,';','설정시간:',A.jisi_h_cold_time,';',CASE WHEN A.jisi_h_cold_cp = NULL THEN '' ELSE CONCAT('설정CP:',A.jisi_h_cold_cp) END) AS cold,
				(SELECT TOP 1 bcf1_cf_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf1_cf_pv,
				(SELECT TOP 1 bcf1_oil_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf1_oil_pv,
				(SELECT TOP 1 bcf1_cp_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf1_cp_pv,
				(SELECT TOP 1 bcf2_cf_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf2_cf_pv,
				(SELECT TOP 1 bcf2_oil_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf2_oil_pv,
				(SELECT TOP 1 bcf2_cp_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf2_cp_pv,
				(SELECT TOP 1 bcf3_cf_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf3_cf_pv,
				(SELECT TOP 1 bcf3_oil_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf3_oil_pv,
				(SELECT TOP 1 bcf3_cp_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf3_cp_pv,
				(SELECT TOP 1 bcf4_cf_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf4_cf_pv,
				(SELECT TOP 1 bcf4_oil_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf4_oil_pv,
				(SELECT TOP 1 bcf4_cp_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf4_cp_pv,
				(SELECT TOP 1 bcf5_cf_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf5_cf_pv,
				(SELECT TOP 1 bcf5_oil_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf5_oil_pv,
				(SELECT TOP 1 bcf5_cp_pv FROM tb_temp ORDER BY tdatetime DESC) AS bcf5_cp_pv,
				(SELECT TOP 1 tf1_zone1 FROM tb_temp ORDER BY tdatetime DESC) AS tf1_zone_1,
				(SELECT TOP 1 tf1_zone2 FROM tb_temp ORDER BY tdatetime DESC) AS tf1_zone_2,
				(SELECT TOP 1 tf1_zone3 FROM tb_temp ORDER BY tdatetime DESC) AS tf1_zone_3
					FROM tb_workheat A
					LEFT OUTER JOIN
						ORDERT B
					ON A.jisi_ord_code = B.ord_code
					LEFT OUTER JOIN
						PRODUCT C
					ON B.prod_code = C.prod_code
					LEFT OUTER JOIN
						CORP D
					ON C.corp_code = D.corp_code
					WHERE jisi_lot = #{lot_qr}
			) AS A
		) AS B	
	</select>
	
	<update id="setMonitoringDataSet" parameterType="workjisi">
		UPDATE tb_monitoring_real SET 
			lot_qr = #{lot_qr}, cutum = #{cutum},
			pum = #{pum}, lot = #{lot},
			pre = #{pre}, chim = #{chim},
			diff = #{diff}, gang = #{gang}, cold = #{cold}
		WHERE hogi = #{hogi} AND hogi_idx = #{hogi_idx}
	</update>
	
	<update id="setMonitoringDataReSet" parameterType="workjisi">
		UPDATE tb_monitoring_real SET 
			lot_qr = #{lot_qr}, cutum = #{cutum},
			pum = #{pum}, lot = #{lot},
			pre = #{pre}, chim = #{chim},
			diff = #{diff}, gang = #{gang}, cold = #{cold}
		WHERE hogi = #{hogi}
	</update>
	
	
	<select id="alarmHistoryList" parameterType="alarmHistory" resultType="alarmHistory">
	    SELECT
	        regtime,
	        a_hogi,
	        a_value,
	        a_addr,
	        a_comment
	    FROM dbo.tb_alarmlist
	    WHERE regtime BETWEEN #{sdateTime} AND #{edateTime}
	    ORDER BY regtime DESC
	</select>




	
	<select id="alarmRankingList" parameterType="alarmRanking" resultType="alarmRanking">
	    SELECT 
	        a_addr,
	        a_comment,
	        a_hogi,
	        COUNT(*) AS alarmcount
	    FROM dbo.tb_alarmlist
	    WHERE regtime BETWEEN #{sdateTime} AND #{edateTime}
	    GROUP BY a_addr, a_comment, a_hogi
	    ORDER BY alarmcount DESC
	</select>
	
	
	<select id="getCurrentAlarmList" parameterType="monitoring" resultType="monitoring">
	    WITH LatestAlarms AS (
	        SELECT 
	            a_addr,
	            a_value,
	            regtime,
	            ROW_NUMBER() OVER (
	                PARTITION BY a_addr 
	                ORDER BY CONVERT(DATETIME, regtime, 120) DESC
	            ) AS rn
	        FROM tb_alarmlist
	    )
	    SELECT 
	        a_addr,
	        a_value,
	        regtime
	    FROM LatestAlarms
	    WHERE rn = 1
	    ORDER BY a_addr;
	</select>
   
   
	<select id="getMonitoringDataList" resultType="workJisiTk">
		SELECT *,
			CASE WHEN hogi_code = '1' THEN '열처리;1호기'
				 WHEN hogi_code = '2' THEN '열처리;2호기'
				 WHEN hogi_code = '3' THEN '열처리;3호기'
				 WHEN hogi_code = '4' THEN '열처리;4호기'
				 WHEN hogi_code = '18' THEN '열처리;5호기'
			END AS fac_name_view,
			CASE WHEN hogi_proc = 1 THEN '진행'
				 WHEN hogi_proc = 2 THEN '대기'
			END AS proc_gb_view,
			CASE WHEN col_name IS NULL THEN ''
				 ELSE col_name
			END AS col_name_view
		FROM
		(
			SELECT 1 AS hogi_code,1 AS hogi_proc
			UNION 
			SELECT 1 AS hogi_code,2 AS hogi_proc
			UNION
			SELECT 2 AS hogi_code,1 AS hogi_proc
			UNION
			SELECT 2 AS hogi_code,2 AS hogi_proc
			UNION
			SELECT 3 AS hogi_code,1 AS hogi_proc
			UNION
			SELECT 3 AS hogi_code,2 AS hogi_proc
			UNION
			SELECT 4 AS hogi_code,1 AS hogi_proc
			UNION
			SELECT 4 AS hogi_code,2 AS hogi_proc
			UNION
			SELECT 18 AS hogi_code,1 AS hogi_proc
			UNION
			SELECT 18 AS hogi_code,2 AS hogi_proc
		) AS HOGI_STD
		LEFT OUTER JOIN
		(
			SELECT ilbo_lot, fac_code,		
				MAX(ilbo_strt) AS ilbo_strt, MAX(ilbo_end) AS ilbo_end,
				ROW_NUMBER() OVER(PARTITION BY fac_code ORDER BY ilbo_lot) AS work_proc,
				MAX(SUBSTRING(ILBO_LOT,10,1)) AS fac_num,
				' ' AS bcf_up_view,		
				MAX(CONCAT('온도:',ILBO_G43,';시간:',ILBO_G42)) AS bcf_pre_view,
				MAX(CONCAT('온도:',ILBO_G23,';시간:',ILBO_G24,';CP:',ILBO_G25)) AS bcf_chim_view,
				MAX(CONCAT('온도:',ILBO_PG6,';시간:',ILBO_G26,';CP:',ILBO_G27)) AS bcf_diff_view,
				MAX(CONCAT('온도:',ILBO_G31,';시간:',ILBO_G32,';CP:',ILBO_G33)) AS bcf_gang_view,
				MAX(CONCAT('온도:',ILBO_G34)) AS bcf_cold_view
			FROM workilbo
			WHERE ilbo_gubn = 'A'
			AND ilbo_end = '1900-01-01 00:00:00'
			AND ilbo_strt >= FORMAT(GETDATE(),'yyyy-MM-dd 00:00')
			GROUP BY ilbo_lot, fac_code
		) AS WORK_DATA
		ON HOGI_STD.hogi_code = WORK_DATA.fac_code
		AND HOGI_STD.hogi_proc = WORK_DATA.work_proc
		LEFT OUTER JOIN
			v_monitoring AS v_mon
		ON HOGI_STD.hogi_code = v_mon.cte_code
		AND HOGI_STD.hogi_proc = v_mon.cte_proc
		LEFT OUTER JOIN
			v_monitoring_temp AS v_mon_temp
		ON HOGI_STD.hogi_code = v_mon_temp.num
		ORDER BY hogi_code, hogi_proc
	</select>

	<select id="getMonitoringDataListStd" parameterType="workJisiTk" resultType="workJisiTk">
		SELECT *
		FROM WORKILBO
		INNER JOIN
			ORDERT
		ON WORKILBO.ORD_CODE = ORDERT.ORD_CODE
		INNER JOIN
			PRODUCT
		ON ORDERT.PROD_CODE = PRODUCT.PROD_CODE
		INNER JOIN
			CORP
		ON PRODUCT.CORP_CODE = CORP.CORP_CODE
		WHERE 1=1
		AND ILBO_GUBN = 'A'
		AND ILBO_LOT = #{ilbo_lot}
		ORDER BY ORDERT.ORD_CODE
	</select>   

   
   <select id="getOverviewData" resultType="java.util.HashMap">
    SELECT *        
    FROM [dbo].[tb_overview_new]
    WHERE id = 1
</select>

<select id="getCurrentAlarms" resultType="map">
    SELECT *
    FROM tb_alarm_current
    WHERE id = 1
</select>
   
   
</mapper>